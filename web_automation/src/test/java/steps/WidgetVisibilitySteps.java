package steps;

import cucumber.api.java.en.Given;
import cucumber.api.java.en.When;

public class WidgetVisibilitySteps {


//    @Given("^All week and (.*) availability selected for (.*) tenant$")
//    @When("^(.*), from (.*) to (.*) and all territories availability selected for General Bank Demo tenant$")
@Given("^(.*)(.*) availability selected for (.*) tenant$")
    public void setUpWidgetVisibilityPreconditions(String dayVisibility, String timeFrom, String timeTo, String territory, String tenantOrgName) {

    }
}
